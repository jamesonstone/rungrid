🗺️ Plan
   project       example-workspace-k7m4q2
   generation    2086f788b27a388562bc
   manifest      .
   workspace     ..
   terminal      headless

🪝 Lifecycle
   before_up     1 command(s)
      ▸ prepare-database (dir=control timeout=30s) argv=["example-infrastructure" "start" "database"]
   after_down    1 command(s)
      ▸ remove-infrastructure (dir=control timeout=30s) argv=["example-infrastructure" "stop"]
   ▸ failure after prerequisites: stop runtime, then run all after_down commands

📚 Repositories (3)
 ┌────────────┬──────┬────────┬────────────────┐
 │ REPOSITORY │ PATH │ REMOTE │ DEFAULT BRANCH │
 ├────────────┼──────┼────────┼────────────────┤
 │ workspace  │ .    │ origin │ <remote HEAD>  │
 │ api        │ api  │ origin │ <remote HEAD>  │
 │ web        │ web  │ origin │ <remote HEAD>  │
 └────────────┴──────┴────────┴────────────────┘

🛟 Recovery
   ▸ start; no recorded lifecycle generation

🧩 Services (3)
 ┌──────────┬────────────┬──────────┬────────────┬──────────────────────────────────┐
 │ SERVICE  │ REPOSITORY │ SOURCE   │ ACTIVATION │ STATE                            │
 ├──────────┼────────────┼──────────┼────────────┼──────────────────────────────────┤
 │ database │ workspace  │ external │ workspace  │ observed only                    │
 │ api      │ api        │ native   │ tab        │ disabled until session ownership │
 │ web      │ web        │ native   │ tab        │ disabled until session ownership │
 └──────────┴────────────┴──────────┴────────────┴──────────────────────────────────┘

🗂️ Artifacts (8)
   ▸ manifest.yaml
   ▸ plan.json
   ▸ process-compose.yaml
   ▸ wrappers/rungrid-resource-guard
   ▸ wrappers/rungrid-maintenance-sync
   ▸ wrappers/rungrid-maintenance-worktrees-prune
   ▸ wrappers/api
   ▸ wrappers/web

📦 Required executables
   ▸ database-ready, example-api, example-infrastructure, example-web, make, npm, process-compose
